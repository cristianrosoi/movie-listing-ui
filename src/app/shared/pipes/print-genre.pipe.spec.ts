import { PrintGenrePipe } from './print-genre.pipe';

describe('PrintGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new PrintGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
