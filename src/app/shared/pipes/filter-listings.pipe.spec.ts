import { FilterListingsPipe } from './filter-listings.pipe';

describe('FilterListingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListingsPipe();
    expect(pipe).toBeTruthy();
  });
});
